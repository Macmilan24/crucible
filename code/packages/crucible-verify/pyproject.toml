[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "crucible-verify"
version = "0.0.1"
description = "Co-evolved generative process verifier (GenPRM/AgentPRM) — the load-bearing hub (P2)."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
authors = [{ name = "Samuel Dagne" }]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/crucible_verify"]
