[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "crucible-gate"
version = "0.0.1"
description = "The composite non-degradation gate: DP holdout + online FDR + reversible canary."
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
authors = [{ name = "Samuel Dagne" }]
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/crucible_gate"]
