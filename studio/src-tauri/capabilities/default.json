{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the Crucible Studio main window: invoke commands and listen for download-progress events.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
